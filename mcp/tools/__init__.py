"""Реализация MCP-инструментов linalg-book-mcp.

- ``context_tools`` — Группа A (предоставление контекста).
- ``verification_tools`` — Группа B (проверки), добавится позже.

Функции здесь — **чистые**: принимают корень репозитория параметром,
не зависят от MCP-протокола, легко тестируются напрямую. Тонкие
``@mcp.tool()``-обёртки живут в ``server.py``.
"""
